{{!< default}}

<main class="site-main">

    <div class="post-feed gh-feed gh-canvas">
        {{#get "posts" filter="published_at:>now-30d" limit="all"}}
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        {{/get}}
    </div>


    {{!-- {{pagination}} --}}

</main>